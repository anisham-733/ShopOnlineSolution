@page "/"
@if (ProductsList == null)
{
    <DisplaySpinner/>
}
else{
    <h3 class="mb-5">Products</h3>
    var productsCount = ProductsList.Count();
    @if (productsCount > 0)
    {
        

       @* const int itemsPerRow = 4;
        var productRowList = new List<ProductDto>();

        for(int i = 0; i<productsCount; i++)
        {
            productRowList = ProductsList.Skip(i * itemsPerRow).Take(itemsPerRow).ToList();*@
                @foreach(var prodGroup in GetGroupedProductsByCategory())
                {
                    //Category headings
                    <h4>@GetCategoryName(prodGroup)</h4>
                    @*Products-childcompo mai prop and parameter*@
                    <div class="row mt-3">
                       <DisplayProducts Products="@prodGroup.Take(4)"></DisplayProducts>
                     </div>
                     <hr class="mb-3"/>
                    
                }
@*        }
*@    }
}